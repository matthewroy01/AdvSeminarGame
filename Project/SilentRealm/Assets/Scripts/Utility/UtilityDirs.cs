using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UtilityDirs : MonoBehaviour {

    // the direction the enemy is facing
    public enum Dirs { up = 0, down = 1, left = 2, right = 3 };

    void Start () {
		
	}
	
	void Update () {
		
	}
}
